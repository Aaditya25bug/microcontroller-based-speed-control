# microcontroller-based-speed-control
In the above project i were used esp32, lcd display, dc motor with quadrature encoder, current sensor, motor driver and other small components. The code is in the next file of the project. If more time permits i will complete the pcb design for the project. 
